server:
  port: 9005
spring:
  devtools:
    add-properties: false
  datasource:
    url: jdbc:postgresql://localhost:5432/fintech_app_db
    username: postgres
    password: taijutsu
  jpa:
    hibernate.ddl-auto: update
    generate-ddl: true
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    
jwt:
  secret.key: secretKey123

mail:
  host: smtp.gmail.com
  port: 587
  username: ${SENDER_MAIL}
  password: ${SENDER_PASSWORD}
  properties:
    mail:
      smtp:
        auth: true
        starttls:
          enable: true
